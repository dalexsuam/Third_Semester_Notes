
10/11/2025
***

# <span style="color:rgb(223, 109, 109)">Serial Peripheral Interface (SPI)</span> 

SPI, or Serial Peripheral Interface, is the new communication protocol that we will use to control the LED matrix on the board. You have probably noticed that this matrix sometimes turns on by itself even when you are not using it, but today we will learn how to properly program it.

SPI is a **synchronous serial communication protocol**, which means two things. First, it is serial: the data is transmitted one bit after another, rather than in parallel across many lines. Second, it is synchronous: in contrast to protocols like UART, the devices involved share a common clock line. The microcontroller, which always acts as the master in the communication, provides this clock, and the peripheral, which acts as the slave, uses this clock to know exactly when to read or write each bit.

One important point is that SPI is typically used only for **short-distance communication**. The reason is simple: while I²C needs only two wires to connect multiple devices, SPI requires several dedicated lines. In addition to the shared clock, **SPI needs one line for data going from the master to the slave, another line for data going from the slave back to the master, and a separate line for selecting the slave you want to talk to.** When you have many devices or longer cables, this number of wires becomes impractical, ==which is why SPI is mostly used for short, point-to-point connections on the same board.==
Despite requiring more wires, SPI offers an advantage that is essential for devices like LED matrices: it is **fast**. Because the communication is synchronized by the clock generated by the master, and because the protocol itself is very simple, SPI can operate at much higher speeds than I²C. This is why it is the preferred protocol whenever large amounts of data need to be transferred quickly and reliably, such as when updating a display.

In summary, SPI is a simple but very efficient synchronous serial protocol used for short-range, high-speed communication. It relies on a shared clock, uses separate data lines for sending and receiving, and communicates one bit at a time. We will use SPI to program the LED matrix because this protocol provides the speed and timing precision required to update the display correctly.
![[Pasted image 20251124081023.png]]

SPI (Serial Peripheral Interface) operates using a **single master device**—typically a microcontroller—and one or more **slave devices**, such as sensors or displays. Unlike I²C, which allows multi-master configurations, SPI is strictly **single-master**, though it can support **multiple slaves**.

The master and the slaves are connected through several dedicated lines:
- **SCLK (Serial Clock):**  
    Generated by the master and sent to the slave. This line ensures synchronous communication, meaning data transfers are coordinated with the clock signal.
- **MOSI (Master Out, Slave In):**  
    This line carries data transmitted **from the master to the slave**.
- **MISO (Master In, Slave Out):**  
    This line carries data **from the slave back to the master**.
    
Because MOSI and MISO are separate, SPI supports **full-duplex communication**, allowing data to be sent and received simultaneously.

- **SS / CS (Slave Select or Chip Select):**  
    This line is used by the master to choose which slave device is active. When the master pulls a slave’s SS line **low**, that slave becomes enabled and listens to the communication.  
    With only one slave, this is simple. With multiple slaves, each slave requires **its own dedicated SS line**, which is one reason SPI is more practical for short-distance communication on a single board.
### <span style="color:rgb(161, 40, 226)">How SPI Communication Actually Works</span>


![[Pasted image 20251124081322.png]]

SPI communication relies on the master and slave behaving like **shift registers**. Each device has a register containing the bits it wants to send—typically 8 bits in simple configurations. During transmission, the master shifts out its data **bit by bit** through the MOSI line. At every clock pulse, **one bit exits the master’s register** (usually starting with the **most significant bit**) and moves to the slave’s input register.

The slave receives these bits sequentially, shifting them into its own register in sync with the clock. At the same time, the slave can also shift data out on the MISO line so the master receives a bit back during the same clock cycle. This makes SPI capable of **full-duplex** operation: both devices can send and receive simultaneously.

Depending on the application, the master may use the returned bits (for example, reading a sensor), or it may simply ignore them. In some cases, the received bits are forwarded (shifted out) to other chained devices, which is another feature SPI enables.

In short, during every clock cycle:

- one bit moves from master → slave on MOSI
- one bit can move from slave → master on MISO
- both shift registers move one position
    
And the data is typically transmitted **MSB first**.

### <span style="color:rgb(161, 40, 226)">SPI Wiring: Why It’s <i>Not Always</i> a “Four-Wire” Protocol</span>

SPI is commonly referred to as a _four-wire_ protocol because the standard configuration uses four signals:

1. **SCLK** – the clock generated by the master    
2. **MOSI** – master-out, slave-in
3. **MISO** – master-in, slave-out
4. **CS / SS** – chip select (active low)
    
However, this four-wire definition is not always accurate. Depending on the device and communication requirements, SPI can use **fewer** or **more** than four wires.

| Three wires                          | Two wires                            |
| ------------------------------------ | ------------------------------------ |
| ![[Pasted image 20251124082000.png]] | ![[Pasted image 20251124082012.png]] |

If the system only requires communication _from master to slave_, the MISO line is unnecessary.  In that case, SPI reduces to only:

- SCLK
- MOSI

This is common when the slave only receives commands but never sends data back.

Some devices add **auxiliary control pins**. Two common examples are:

- **RESET** – allows the master to reset the slave device hardware.    
- **OE (Output Enable)** – controls when the slave’s output is actually driven to external hardware.
    
The **Output Enable** pin is particularly important in systems like an LED matrix.  
Inside the slave device, the shifted data may be progressively updated bit by bit as it is received. If this internal data were immediately connected to the LEDs, the LEDs would flicker or show random patterns during every shift operation.

To prevent this, the OE pin ensures that:

- while SPI communication is happening, the outputs remain disabled
- **only after the full data frame is received**, the slave enables its outputs and applies the new data to the LEDs
    
This guarantees stable, flicker-free updates. So in practice, your SPI setup might have:

- the standard communication pins
- plus additional control pins such as RESET or OE
- depending on the chip’s requirements
    
This is why SPI is _often_ called a four-wire protocol, but in reality the number of wires varies.

### <span style="color:rgb(161, 40, 226)">Multi-Slave SPI: How Multiple Devices Connect to One Master</span>

SPI allows one master to communicate with several slave devices. There are two main ways to wire this: **independent-slave wiring** and **daisy-chain wiring**.
#### <span style="color:rgb(2, 141, 192)">1. Independent Slave Configuration</span>

![[Pasted image 20251124082647.png|400]]
In the classic SPI setup:
- **MOSI** is shared by all slaves
- **MISO** is shared by all slaves
- **SCLK** is shared
- **Each slave has its own CS (Chip Select)** line

Only the slave whose CS line is pulled **low** becomes active. All other slaves place their MISO line into **high-impedance** so they don’t interfere with communication on the shared data lines.

This prevents collisions—for example, if two slaves tried to drive the MISO line at the same time, you would get corrupted data.

**But there is a drawback:**  
Each slave needs its **own CS pin**, and wiring becomes unmanageable very quickly.  
With 3 slaves, you already need 6–7 wires. With 10 slaves, it becomes a wiring nightmare.
#### <span style="color:rgb(2, 141, 192)">2. Daisy-Chain Configuration</span>

![[Pasted image 20251124082824.png]]

To solve the wiring problem, some systems use a **daisy-chain** layout. Instead of sharing MOSI and MISO across all devices:

- MOSI of the master → MOSI of the _first_ slave
- MISO of the first slave → MOSI of the _second_ slave
- MISO of the second slave → MOSI of the _third_ slave
- …and finally the last slave’s MISO → master’s MISO
    
All devices share the **same CS line**, so they all shift data at the same time.

***Why is this useful?***

Because SPI works like a **shift register**, this setup lets you send a _long chain of bits_ that passes through each device in sequence.

For example, with 8-bit registers:

- To program only the first chip → you still shift 8 bits for each device
- To program the third chip → the data must pass through chip 1 and chip 2 first
    - 8 bits × 3 devices = 24 clock cycles
    
So the chain behaves like one long shift register.

***Advantages***
- Much **fewer wires**
- Only **one chip select** needed
- Cleaner PCB routing
    
***Disadvantages***
- **Slower updates**, because data must pass through all devices in the chain
- You always shift through _every_ device, even if you're only targeting one






However, we can also, since we said that the data are transmitted based on a clock signal, we also have multiple other things, not just the two configurations or maybe the multiple pins. The SPI protocol is not such a standard protocol like the I2C or the UART. Those protocols are always the same. The SPI can be actually personalized, let's say, depending on the sensor we are using. For example, first of all, we have the clock polarity, which is going to tell us which state of the clock signal is considered as idle. So, for example, we can decide that the clock is idle when it is low or it is high. What does it mean? It means that when the communication begins, the clock will go from the idle state to the other state, which is active. And of course, this can be from low to high or from high to low. And depending on the architecture inside the sensor, we could have the polarity to zero to one, okay? So this is something that we have to program, for example. And on the other end, we also have the clock phase, which is going to tell us when the data is actually read. And it is, of course, relative to clock transitions. Why? Because inside we have nothing but some flip-flops, let's say. And in particular, as you can see here, this is, I think, a little bit of a reminder for all of you, but I think you are all familiar with the way flip-flop works. In this case, for example, it's a small example. We have that when the clock is high, we actually have the data being sent from the data line to the queue. And of course, we also have the inverted signal available on the negated queue pin, which is something that we actually see from the outside like this. On the other hand, imagine what happens when the clock stays high and we have data coming in from here. What we actually want to do is to do something like this. I'm just moving the window from the class. What we can actually do is going to use multiple latches one after the other in order to fix the issue of having Q changing due to the data line changing when the clock is high. So, what we do here is we have a consecutive a 2nd, let's say stage, which allows to have the data changing only on the transitions of the clock. So in this case, of course, it's considered when this one is high. So it's not on the edge of the clock, but on the signal. In this case, we actually have the edges being considered. In particular, this Q will go to the data value when the clock goes high. And on the other hand here, we actually have the output of our chip being the one from the data only when the clock goes back to low. Why? Because we have a negated here port. Why is this useful? Because again, we can select based on the architecture of the flip flop inside, we can actually decide if the data are sampled on the leading edge, which means on the rising edge or on the trailing edge, which is over here, the falling edge. Why this? Because in that way, we know that when the clock goes too high, for example, we can have the output or the opposite is true. And imagine all of these combinations together. So we have that the clock is idle when it's low, and we can have these two options, or the clock is idle when it is high, and we have these two options. And this leads to having actually all of these combinations of modalities, which are related, of course, to the interpolarity and the phase of the clock. and the clock idle state. Why this? Because in this way we are actually able to have some specific reading outs of the signal, which is good. On the other hand, it causes a lot of confusion because we always have to make sure that the two chips, master and slaves multiple, are actually talking in the same way. Because if one of these parameters is different, the communication will not come out correctly. So, of course, you don't have to learn all of them by heart. However, I just suggest to you to look at this and try to understand what happens here on the rising edge and leading edge of the SPI and when the data actually are sampled in these four cases. Again, the pros and cons of the SPI, of course, are the fact that we have the full duplicate communication, which means that we can have sending and receiving on data at the same time. Of course, we have a high speed in this case, because we are not talking about like the last time with RC discharges. And this means that we have higher speed. And we have a lot of protocol flexibility because it is not related to 8-bit words. It depends, of course, on how much data we send online. And in this case, of course, the hardware interface is usually simpler. But on the other hand, we also have some issues. For example, we have a lot of pins because we need many things, not just the data and the clock like the S4C. Also, we do not have a slave acknowledgement, okay? Because the master is sending it out, but we do not know where it's going. And finally, usually we only have one master device, which is a problem in some cases, not in our case, but it is important to know it. And finally, it only handles short distances because all of this data being sent from one to the other could actually be corrupted because of course, this data need to go through a lot of different chips and we could have some issues. And finally, Of course, we have so many existing variations that it makes it difficult to have a specific development tool, which is the same for everything. If you remember just the I/C that we have a few things to program, like for example the baud rate, in this case you have so many things to program on the I/C that it could lead to some compromises. And as you can see here, if my computer works, okay, in particular on our board, we have four wire-configurations for Q2X communication with up to 42 megabits per second. You can see that the speed is very high, actually. It is not low like the SQC. We are talking about not any more low speeds, but very fast speeds. On the other hand, of course, depending on which SPI we use, we have the 1 or the 4, which can go up to 42 Mbps, but the 2 and 3 are actually limited to 21. And finally, all of them are related to the DMA. So this is something important because we can actually use the DMA in all the lines on our board. Here is a screenshot of the things that we can set. And in this case, just one thing, the interesting thing is that the frame format is named Motorola, mostly because the SPI was related to, the creation of the SPI was related to Motorola. And I think you can understand why, because they needed some kind of fast protocol, which was not good, did not exist. We had the I2C, we had the UART, these are all too slow for a cell phone, let's say. So what they did was coming up with this new protocol, which is quite faster. And as you can see here, we can actually select what we said before. So, for example, the polarity and the phase. Here, the prescalar, please move it to four, because in our case, if you leave it to two, you could have some issues in the communication with LEDs. So this is the only thing you have to modify, let's say, in the parameter setting of the SPI. And as I said before, when we are correcting the homework, in our case, we will not have to receive data because we're only programming the LED matrices, which are actually controlled by two shift registers. And in this case, we will have to transmit data. Of course, the transmit is going to be sent out, and it's kind of this usual parameters that we see. So we have which SPI, the data buffer pointer, the size to the data. the data and the timeout. And of course, then we have the DMA version, which is exactly the same. However, we're going to do that in the timeout and the data are transmitted in DMA mode. So what actually happens on our own board? Because as we said, yes, we're going to use it for LED matrix, but we don't know actually what happens. The LED matrix is over here and you can see that you also have two chips, which are quite big, which are shift registers. And these shift registers actually talk in SPI. In particular, they are 8-bit serial in parallel out shift registers. So it means that we will program them in SPI and then the data will be sent out in parallel to the matrix. As you can see, the pins of these registers are, of course, the serial data input, which is the data coming in from the microcontroller, and the clock, In particular, the shift clock, which is the one that we talked about before, which on each rising edge of the clock, the data is shifted into the shift register. So with each clock cycle going high, we have actually the data being shifted in their shift register inside of here. Actually, as you can see here, they're not simply and we don't have inside of each ship. This is just a picture of one of them. We don't have just one ship register, but they are copied. We have one over here, which is on top, which is exactly what I showed you before on the slide. But then we have another one here on the bottom. Why? Because you want to make sure, as I said before, that while I'm reading out the data that are being shifted and saved in the shift register here, they are not actually sent out to the LED before, let's say, they are completed. And the output actually is enabled here on the second stage, let's say, of the shift register through this pin, which I don't know if you can see, but it is named STCP, which is over here, the store clock. The store clock is used to actually get the shifted data and send out to the output register. So we latch the data and we send it to the parallel output. And in this case, again, it is transferred to the output pin of the rising edge of this clock. In particular, we will not use this one as a clock, but we will program it as a pin, as an output pin, a GPIO output, telling it when it goes high, It means that we actually enable, let's say, the output. Actually, this chip also has an output enabled, like I told before, which, again, does another check. So in this case, in particular, it enables or not the parallel output. So not only the data are shifted, but then these ones, let's say, are open or not. Okay, a simple way to do it. Then we have the eight output pins, which are the ones that are connected to the matrix. And then also we have a serial out. So in this case, for example, if we disable the output enable, in this case, the outputs could be disabled, but we have the output in parallel being in serial, sorry, which can still be read out. And in our case in particular, our two shift registers, which are over here again, are connected, it's a different image of the same thing, are connected in daisy chain mode. So it means that we have two shift registers which will need both to be programmed in order to have the LED matrix to be programmed correctly. I'm talking a lot about the LED matrix, now we can actually see on the schematic what happens. In particular, here we have the SPI from our microcontroller, We have some LEDs which can be used to see if the communication is working or not. And then we have the two shift registers which are connected, as I said before, in daisy chain mode. And in particular, one of them controls the rows of the LED matrix and one of them controls the columns of the LED matrix. So in this smart way, just by using these two, we can actually program the whole matrix, which is quite big, as you can see, because we have five columns and seven rows. So it means that we can use a lot of different elements on this LED, for example, a lot of drawings or characters or whatever you want. So how do we actually light up one of these LEDs? These LEDs, as you can see, are connected on one part here to these lines, which means that whenever this one bit, for example, is set to one, the whole row will be, let's say, enabled. And here is the same. So whenever this one goes to one, we actually bring down this pin to ground. And what happens is that, again, the whole column could be enabled. So by combining the programming of this shift register and this one, we actually can select which ones to turn on or off. However, for example, I want to turn on the LED in position here, the first row and first column, and the one here, for example. So I'm talking about the second column and third row. So what happens if I actually turn this one to one? So I enable this one because I want this one, the first row to be enabled. And then I also enable both columns. What happens is that we'll have both columns completely, both pins completely enabled. So I will not just have this one and this one, for example. I will have this one, this one, and then also these two. Because what happens is that I'm selecting both rows and both columns. So you will see, we have to understand in the homework how to actually complete this, and there are some slides I will show you now. In order to actually have a character being displayed and not having issues with the activation of the LEDs, what you will have to do is to multiplex the multiple columns. So in particular, what you will do is you will actually write the first column, then put it to zero, write the second column, put it to zero, and so on. How can this actually make a character appear on the LED matrix? This can happen because if you're fast enough with turning on and off, the human eye actually does not see the flickering of the columns, but sees just the light as stable. And in particular, you will see that there's a value in the next few slides. So what you will do is actually enable the first column with the rows that you care about, disable it, enable the second column with the rows that you care about, then the third, the fourth, the fifth, and then go back to the zero. So there's like a multiplexing of the columns, and on the other end, the rows will actually be, select which pins you want to, which LEDs you want to turn on. Here is some other descriptions. I think all of you know how the LED configuration works with the resistance, so I don't think I need to explain it. And here is an example again on how the LEDs are turned on. Okay, so column and row combination. And here is a description on how to transfer the data. So as I said before, since we have a daisy chain configuration, we need to send out 16 pulses, not just 8, because we need to program two shift registers. And also, we need again, eight to do add the column and eight, of course, to add the other shift registers. And here is an explanation of how to write it on each column. And then you have the auto-enable pin to be selected. Here is a description that you can only write one column at a time and why and in particular you can have a refresh rate above 50 hertz. Above 50 hertz, so below let's say one column every four milliseconds, is enough for the human eye not to see this turning off and on of each column but we will actually see the image as a stable image even if actually we are flickering the LEDs. And here is an example of some of the characters that you can actually use. I uploaded an excel file which you can use and here is the explanation. You can actually fill in with an x the drawing of the matrix and it will tell you what you have to write on the columns and on the rows. So this is useful because in that way you can actually program the two registers easily in order to have the light up dots that you want. So the theory is over. I know it was a lot today, but I think that you will have a lot more time to actually work now because you have one hour and you already can start working on the homework, mostly because this type of work is a bit more complicated maybe than other. So what I want you to do is you take a letter, you transmit it to the LED matrix using the SPI and with a timer interrupt, don't use the ALDILAY. Of course, when you start trying to communicate, start with the delay. OK, but when you deliver the homework, we want to see, of course, with interrupts and everything. And I want you to alternate between two letters or one letter and one symbol or whatever you want. Okay. So I want you to have your matrix displaying, for example, I don't know if your name team name is A1, you first display A and then you display the one. Okay. So I want the end and goes back to A and one. So I want two characters to go on the matrix. And you have to understand how to do the multiplexing with the SPI, which I was talking to you about. Remember that you also need to enable actually the data going out. Otherwise, you will not have the data being displayed on the LED matrix. Again, also for the SPI, start by working without the DMA, of course. But then when you deliver the homework, please have the DMA in this case as well. Okay, so timer interrupt and DMA. Any questions from the class? There are no questions? Okay. From the previous slide, we have to write the the register, then we push it, and then we write the form right to the right. No, what you do is you send, it's like a communication of 16 bits. Instead of sending out a communication of 8 bits and then another one of 8, you transmit 16 bits. Because imagine it's as if you have one single chip having a register of 16 bits. because of the daisy chain communication. If it was not in daisy chain communication, you would have selected one chip, talked to it in 8 bits, then selected the other chip and talked to it in 8 bits. But in this case, it's as if it's one big shift register, okay? Having 16 bits one after the other, which you can see here better, maybe. Okay. So you can also see maybe here, just a second, this is more clear because it's like a little schematic and it's a little bit here. Okay? So imagine this one then goes back to... What you want to do is to write both of them and to write both of them you have to send out 16 bits because 8 go here and 8 go here. Yes, but also I think that the first shift register is connected to the serial line to the row shift register. So first we need to push the 8 bits for the row register and then the bits for the column. Yes, of course, you send out, of course, when you send out the data first, you send the data for the second connection and then to the first one. Okay, so you send out first the data to the one farther away. So in this case, as you can see from the drawing, just give me one second. You first send out the data. I'm trying to find something more clear, which I can show you with a drawing, but... Maybe here it's better. Of course, you send in first the one connected directly to the microcontroller, and then you send out... Sorry, first you send out the data connected to the one not directly connected to the microcontroller, and then to the one near microcontroller. Because the bits will have to travel more, let's say, to the second one than the first one. Okay. So it is not two communication, it is a single one, but of course you have to think as if the first eight go to the farther one and the other eight go to the closer one. Okay, I will be here online if you have other questions or something, but just let me know, Ilaria, if you need it. Good work. Thank you. I'm sorry. um You're welcome. I I don't know. I don't know. Oh, okay. Thank you. ... ... ... ... ... ... ... ... Yeah. ... ... ... ... ... ... ... ... ... ... Now, as I said, we have a lot of issues. We have a lot of issues. We have a lot of issues. We have a lot of issues. - Thank you. - All right. All right. Yeah. um Can you tell me if it's human? What is the color of the phone? Can you tell me if it's for ESS? Thank you. ... ... ... ... ... Thank you. ... ... ... ... ... ... You can see the same. You can see the same. ... Thank you. um Thank you. Thank you. Thank you. um - - - - um um ... ... ... ... ... ... ... Yeah, maybe it'll be easy. Yes. um Because there's not even six years. It's in the middle of the century. And the men should have worked on it. Jacky, it's a night in the woods. Yes, it is. It's a night in the woods. And that's not it. Yeah, I'm tasting it. No, I'm not. *Sigh* Yeah. Yeah. Okay. I'm going to be a woman after all. I'm going to be a woman after all. I'm going to be a woman after all. I'm going to be a woman after all. Should we do this one? Yeah. Okay. Okay. Okay. Okay, thanks. So, three people. Thank you. ... No, no, it's the other one. You have to first select the A5. It's the green one. It's the one that's actually... And now you can do it with the other one. How? No. Oh yeah, I said it. Here we go. Yeah, the PA5. Not people, it's not people. It's... Yeah, whatever. ... ... ... ... ... ... ... ... ... ... ... ... Okay. Yeah. Thank you. Thank you. um ... ... ... ... ... ... What is the idea of the U.S. Embassy? Thank you. So Thank you. ... ... ... ... ... ... ... ... ... Thank you. Thank you. um *Mumbling* Because it was a little bit more... Fortunately, it was a little bit more expensive, but... It was worth it. It's just... It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. It's worth it. - Thank you. Thank you. um um I should cut? Yes. I think I cut. Oh, I didn't see it. Sorry. It feels like a normal, it feels like a fast, and it feels like a normal. And it's not the same. It's not the same. It's not the same. It's not the same. - ... ... ... ... ... ... ... ... *inaudible* No. *Tap tap* Oh wait, I need another set of time. Then, say, I'm in here. I'm going to pause the situation. Thank you. um Yes. So, Thank you. Okay, so we have to... Okay.

You've run out of transcription time. This content has 8 more minutes that need to be unlocked. Upgrade to see everything.

