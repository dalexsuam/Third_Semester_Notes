
09/10/2025
***
![[Pasted image 20251009084208.png]]

To conclude. This is a modified U-NET, this one an additional branch, a decoding branch but both are devoted to two different tasks. One is devoted to segment a certain region and the other is taking the border. We perceive arrow coming from the bottom to the top, the idea is to explore the bottom layers to improve the overall segmentation of the upper part.

This approach is helping also in case we're changing the task for classification, denoising, increasing resolution. It makes the net more versatile.

Next topic..

![[Pasted image 20251009084651.png]]

Generative adversarial networks (GANs)

This is correlated with the generative models we've addressed in previous classes specially when we talk about decoding. If you recall we started by an input and the idea was to decode this input into a domain w.r.t to the input and in general we understood when we merge together a decoder with an encoder, the input of the decoder is the output of the encoder... We're here addressing the model without addressing the encoding part, we have no idea about the input and we need to make some choices about the nature of the input and again we have to consider a diff kind of trainning system that is specially tailored by the concept of adversarial activity. Were now describing it in details.


Ok, let's recall, i have a latence space decoded and generate an output.  In this domain we're assuming our model is able to learn statistics of the data or a distribution of the data, and the simplest is the gaussian distribution. We might sample our distribution to our convenience, because we have the analytical model. But, if we have a bunch of complex data, like biomedical images, can't i have a simple distribution? It is hard to understand the mean value of a bunch of images. it losses the meaning of each image in the set we are convey, on the other side what is the variance? So, it is difficult to think this kind of data in a traditional distribution, data is pretty complex and the information its convey, so we might convey a multidimensional distribution. 

We could, on the other side, we can understand what is the mean in this dimension w.r.t this dataset, if we have another dimension, and so on and so forth and each dimension will be associated to a variance.

If we're in the domain of number, we use a distribution of numbers, but as soon as we're obtaining complex data this approach is not longer useful, and that is why generative models are truly useful in contrast for modeling this kind of data, the idea is to learn statistics, all data is linked together with not-known statistics and this model is able to learn how this statistic-work

Good stuff abt statistic is that we might infer new data, this is where the idea of generation comes from, data that was not used to train the model. However, this approach has some sort of limitation.



![[Pasted image 20251009085000.png]]

What is the principle? it relays on the availability of data, I need data, the model is a generative, layered, decoding model, and in order to train it we need training data, and that short cylinder, we see it is the container of our dataset, if i pick one image from that, and getting the sample, and in the mean time i have my generative model in the blue box, my statistics, i sammple my statistics (random variable) and I might generate 1 sample. But, recall, at the beginning we don't have the exact model, cos in the beginning we don't know anything about our model. Then, we need to initialize the weights. I'm not sure at the beginning the model is able to produce an imitation of the true data, basically the generative model will be in charge of imitate the dataset. so, I have my model, I sample my data and then I have my true data from the sample of the image dataset and I'll try to understand if they're the same.

So, that the model is not learning the shape of the model but it is learning the latence space. If they see both samples are similar, it is still to be learn cos after that there will be some different samples and at the end of the training we'll have an encoding corresponding to the image dataset, and the difference between the data generated by the model  and the corresponding data should be close. 

If I have 100 images of my data set and i'm using 8 bits of encoding, we have 256 diff realizations, the subset of 100 are exactly the image dataset, and the remaining are new data we can obtain, that were not included in the image dataset. I'm not sure if the training is sucessful? Should I verify if the 100 and 156 are ok?, realisticly? this is one aspect, the other is related to the sampling of the encoding space.

If we' are taking a sample from let's say a binary array $[00100100]$ and we wanna generate the $[00100101]$ (the next one) would it show continuity from the image dataset? If the training was successful I could say yes, if it is consistent I can generate the remaining 156 data...

The fundamental idea to let the model to learn the statistic of the data is to exploit this concept of competition. It is competing with the training set, during training it is trying to imitate the data. 
![[Pasted image 20251009091443.png]]

This model (the box Generator), goes with the term generator. This will be called from now on generator, pretty intuitive cos it is able to do so, and the input of the generator (its domain) is called the latent space z.

![[Pasted image 20251009091812.png]]

This is the architecture of the training. We have a model that is the generator and we have afterwards a referee the image dataset more or less or and an advanced trainer called discriminator that will let know if the generator is able to replicate it or not. The principle is that the generator and discriminator are both Neural networks, so, the generative a... N... is training both, the idea is that at the beginning the discriminator don't know how to judge correctly, and this is why during the training both are learning, what would be the idea of supervision here?

The discriminator will start by knowing the image from the dataset, but doesn't know its statistic, and it will be tagged as real, if doesn't come from the image dataset, it is fake. It'll be improved during the training and will start to smooth out the difference between real data and fake data. From the beginning the discriminator is pretty confident knowing what data is real and fake (image dataset or generator), after training it will show more uncertainty, that is the basic idea.


![[Pasted image 20251009092316.png]]

The discriminator is a network and it is a judge (a classificator) it says if data belongs to real or fake class, the input of this discriminator can come from either the image dataset or data generated from the generator. These two would be the input of the discriminator, alternatively, in one moment we'll get true data and we'll produce an output and another moment we'll get from the generator and produce also the output.

![[Pasted image 20251009092449.png]]

The idea during the training is to extract an element from the dataset, provided to the discriminator, it is a network that performs the classification and we can see it could be a CNN, with ending classification task, and then we're expecting the discriminator says it is true data.

![[Pasted image 20251009092538.png]]

Then I have my generator, I have my latence space, and then it is entering to the generator and it will produce an output. This would be hopefully the final result but at the beginning the generator doesn't know anything about the data, we can just randomly initialize the weights! The output will be whatever at the beginning. A bunch of bits without any smenatic information

![[Pasted image 20251009092713.png]]

I'm taking one z, we go to the discriminator and it marks fake, till the generator finishes its learn and marks real and it has fouled the discriminator. 

![[Pasted image 20251009092900.png]]

In terms of architecture, (10 years ago was introduce, Goodfellows), so, we have a decoding model for the generator and an encoder model as a final classification task. So, in general we start from a latence space with low dimensionality and layer by layer in the generator we're learning how to up-sampling and how to converge new information, this means the generator is learning the convolution filters from unsemantic info to semantic data for our understanding, introducing dimensions

![[Pasted image 20251009093150.png]]

What is the training? We have two networks, the discriminator and the generator. The generator need to maximize the imitation of data and the discriminator need to minimize the difference between the data. So, one mechanism is trying to increase the difference in the classification, and the other mechanism that is trying to improve the generator is trying to decrease the difference in the classification. so, 1-> TRUE, 0->False for the discriminator, and at the end of the training we would from the discriminator to be totally uncertain that is in 0.5.

![[Pasted image 20251009093537.png]]

This point is called the Nash Equilibrium.

This is needed cos otherwise, out of the equilibrium might lead to overfitting cos it might just replicate the data from the training. When we're in an acceptable range, we need to stop the training. 

![[Pasted image 20251009093918.png]]
This is 2016, this is a network that is able to generate images of house interiors. This means the generator was trained with a bunch of images that would be true, and after sucessfully trained it was able to generate good images representing house interiors. This images were not in the training set, these are completely new. 

So, if I'm having a pretty large dataset for training, I'll need a larger encoder. If we're dealing with very large encoding space and we have few data, 32bits, and 100 samples of the data, probably it is learning in a pretty small area of the encoding, and probably that we won't produce something coherent with the data. 

![[Pasted image 20251009094306.png]]
CycleGAN 2017, an evolution of the GAN, this model is taking an input. The generator is not longer a decoder, but encoder-decoder and it is able to take an input an translate the picture into a paint of each of the different painters :)

![[Pasted image 20251009094414.png]]

Here, we have an image, and we're mapping a zebra in the same horse.
The training might be a lot of videos/images of zebra, in this case the generator is not just a typical decoder but encoder-decoder network, so, input is a horse and output is a horse mapped as zebra

![[Pasted image 20251009094616.png]]

The idea of learning face features so we're able to sample the encoding space and generate the corresponding image. If we have very complex data, like faces, probably we're not sure if we're taking elements from different areas of the encoding space. 

![[Pasted image 20251009094805.png]]
Another example, to increase the resolution. Start with lower resolution and generate the same date with increased resolution. 

![[Pasted image 20251009094937.png]]

This is different, cos the input wasn't an image but a request from a sentence. There is a model that maps the sentence, into a numerical encoding and this encoding is the input of the generator.


![[Pasted image 20251009095219.png]]

Possibility to use the GAN in biomedicine. Applications?

Data translation from one domain to another domain, from a CT-space to an MRI-space. Starting from MRI and obtain the corresponding CT. Take into account that this is the possibility, but you need data, to let the generator to learn the correspondence, there are a lot of variability, atomical/pathological... so on, you cannot just train a model like this using 100 cases, we cannot really converge.

In our field is complicated to collect the data, we need a lot. 

Also style transfer,

for protein and gene synthetic data bank. To stand relations between a lot of genes for use to corresponding protein. The idea, the main ambition is to study tercerali and quaternali dimension of proteins and try to generate new proteins according to specific spacial contrains that are useful to map new strap, active strap might be more effective 

Another example, is enlarging the dataset, in order to perform some models. That is the point. 

